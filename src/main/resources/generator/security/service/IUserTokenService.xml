<?xml version="1.0" encoding="utf-8"?>
<template>
  <name>IUserTokenService</name>
  <filePath>src/main/java/${packageName}/security/service</filePath>
  <fileName>IUserTokenService.java</fileName>
  <content><![CDATA[
package ${packageName}.security.service;

import ${packageName}.security.dto.UserInfo;

public interface IUserTokenService {
  
  public void put(String token, UserInfo user);

  public UserInfo getUser(String token);
  
  public String createToken(UserInfo user);
  
  public void deleteToken(String token);
}

]]>
  </content>
</template>